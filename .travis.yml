language: android
jdk: oraclejdk8
android:
  components:
    - build-tools-23.0.2

notifications:
  email: false

before_script:
 - chmod 777 gradlew

before_install:

  # for gradle output style
  - export TERM=dumb

  # environment info
  - ./gradlew -v
  - uname -a

# Let's try to build...

script: ./gradlew clean build